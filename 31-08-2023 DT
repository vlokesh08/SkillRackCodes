#include <bits/stdc++.h>
 
using namespace std;

int main(int argc, char** argv)
{
    int a,b,c;
    cin>>a>>b>>c;
    int ans=0;
    for(int i=3;i<=a;i++) {
        if(i+1<=b && i+2<=c) {
            ans = (3*i+3);
        }
        else{
            break;
        }
    }
    cout<<ans;
}
